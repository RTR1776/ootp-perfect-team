# Handoff — 2026-09-26 (Claude Code cloud session)

Read after `Docs/HANDOFF - read me first.md` (who L.J. is, scoring rules).
Everything here is merged to `main` and live unless it says otherwise.

## Access (new as of this handoff)

L.J. is adding two variables to the cloud environment. A session only sees
them if it STARTED after they were saved. Check presence without printing
values: `[ -n "$OOTP_SITE_PASSWORD" ] && echo set; [ -n "$DATABASE_URL" ] && echo set`.

- `OOTP_SITE_PASSWORD` — the site password (the app's `APP_PASSWORD`). The
  live app is https://ootp-command-center.vercel.app (Vercel project
  `ootp-command-center`, team `lj-coxs-projects`). Log in with Playwright
  (Chromium is preinstalled, `executablePath: '/opt/pw-browsers/chromium'`
  if needed): POST the password on `/login`, the session cookie lasts 30
  days. L.J. granted full access, but read and screenshot unless asked to
  change something. First job: load a saved roster on /build and screenshot
  the Field view and the Recommended buys box (never seen live yet).
- `DATABASE_URL` — Neon. With it, the repo's pnpm scripts run directly
  (`pnpm results:log`, `pnpm cutoff:project`, imports). Without it, use the
  Neon MCP (`project billowing-glitter-35624732`) and hand-written SQL, as
  every session so far has.

## Workflow L.J. has authorised

- Work on branch `claude/funny-brahmagupta-cm7oor`; reset it from
  `origin/main` after each merge. Open a PR, wait for the Vercel status to go
  green, then merge it yourself — L.J. owns the repo and said "push it".
- `web/` lint has 15 pre-existing errors in roster-builder.tsx and
  build/page.tsx (react-hooks rules). Don't add new ones; `tsc --noEmit`
  must be clean.
- Scratch scripts: `web/scripts/.scratch/` (gitignored).

## Standing preferences

- Data is data: recent weeks matter most, no hot-hand stories; small samples
  (a couple hundred PA) never overrule thousands.
- Defense matters, priced in runs; no hard glove floor beyond a sanity floor.
- Tourney data comes from the cwhit site only (the all-stats splitter was
  built and then removed at L.J.'s request).
- Weeklies can change era/park under the same name: `restrictions.formatSince`
  on the tournament drops that event's older exports.
- Results: when L.J. pastes a Your Tournaments screenshot, log new rows
  (dedupe on event id), skip PD-Q quick events, save the rows to
  `Tourney Data/PTCS7 Results/<date>.txt`, and report standing vs the lines.

## Where things stand (Sep 26)

**PTCS 7** (ends Oct 4). Safe: Cap, Gold, Diamond. Silver +3 over the line
and PD Weekly exactly on it — protect both. Focus: Open (needs ~31), Bronze
(needs ~62, above current pace). PD Daily, Iron, Live out of reach. Lines
re-project after Monday's community dump (Load Tourney Dumps, then
`cutoff:project --write`).

**PEL — Kansas City Torrent - JW.** 8 back of Columbus with ~21 to play;
only a division win keeps them in PEL, so they expect to drop to HD.
- vs RHP: De Vries 2B, Piazza C, Manush LF, Hidalgo CF, Banks SS, Aaron DH,
  Clark 1B, Beltran RF, Rolen 3B (Wood off the roster).
- vs LHP: Banks SS, Aaron 1B, Ott RF, Piazza DH, Boone 2B, Gibson C,
  Manush LF, Beltran CF, Rolen 3B.
- Rotation: Cy (variant), Hershiser, Saberhagen, Stieb, Haddix; Lee variant
  long man. Line up Cy/Hershiser/Saberhagen for Columbus Sep 24–26 at home.
- Pen plan: drop Henke and Vida Blue; keep Britton (CL), Nen, Kimbrel,
  Gossage, Palencia, Lee V, CC. Buy candidates: Willie Hernandez (~243k,
  +6.0), Kenley Jansen (~97k, +4.3 value). Bryan Harvey (new LE) — wait.
- Next-season plan: buy Canseco (~200k) first, then J.D. Martinez or Kevin
  Mitchell as the vs-LHP DH. Park: they pick their own; roster is
  right-handed power, so a park that boosts RHB HR and suppresses LHB
  (Fenway 1945, Polo Grounds 1911, Bennett Park 1911) — Yankee Stadium 1987
  is about the worst fit. Rerun on the actual park list when they have it.

**Saved rosters in the app:** #23 Saturday Negro Leagues Slots (tournament
779, now restricted to Negro League Star cards; cheap buys Dobie Moore,
Jud Wilson, Lazaro Salazar ≈150k lift it from +48 to +89), #24 Saturday
Iron Warriors (534, ratings only).

## Shipped this session (all merged)

- #16 File OOTP Exports: end-of-run report opens in TextEdit (the modal
  outgrew the screen and its OK button was unreachable).
- #17 results through Sep 25.
- #18 Build page: Recommended buys box (biggest adds / best value / variant
  upgrades, shared `rankShop`), estimated variant offers (+5 hitting / +3
  pitching, priced at the variant's last-10), roster header counts players
  vs roster size, one-line data-confidence strip.
